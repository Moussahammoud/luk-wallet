# LUK Wallet

Responsive expense tracking + budgets app (no login; data is stored locally in your browser).

## Run locally

```bash
npm install
npm run dev
```

## Features

- Add / edit / delete expenses
- Monthly view selector
- Category budgets with progress bars
- Search + category filter
- Export / import JSON backup
- Reset button (clears local data)

